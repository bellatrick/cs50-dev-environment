{
    "name": "CS50 Development Environment",
    "image": "mcr.microsoft.com/devcontainers/universal:linux",
    "features": {
        "ghcr.io/devcontainers/features/python:1": {
            "version": "3.10"
        }
    },
    "postCreateCommand": "sudo pip install --upgrade pip SQLAlchemy cs50 check50 submit50 style50 pytest black && sudo apt-get update && sudo apt-get install -y build-essential gcc gdb make valgrind && sudo sh -c 'cd /tmp && git clone https://github.com/cs50/libcs50.git && cd libcs50 && make install && rm -rf /tmp/libcs50'",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python",
                "ms-vscode.cpptools",
                "ms-python.black-formatter",
                "ms-python.flake8"
            ]
        }
    }
}
